name: Release

permissions:
  contents: write

on:
  push:
    tags:
      - "v*"

jobs:
  create-release:
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          draft: false
          prerelease: false
          generate_release_notes: true

  build-assets:
    name: Build Release Assets
    needs: create-release
    runs-on: ${{ matrix.config.os }}
    strategy:
      matrix:
        config:
          - { os: ubuntu-latest, target: x86_64-unknown-linux-gnu, suffix: "" }
          - { os: macos-latest, target: x86_64-apple-darwin, suffix: "" }
          - { os: macos-latest, target: aarch64-apple-darwin, suffix: "" }
          - { os: windows-latest, target: x86_64-pc-windows-msvc, suffix: ".exe" }

    steps:
      - uses: actions/checkout@v4

      # ISPRAVAK: Koraci za izgradnju frontenda sada koriste 'app' folder
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'
          cache-dependency-path: 'app/pnpm-lock.yaml'
      - name: Install App Dependencies
        run: pnpm install --frozen-lockfile
        working-directory: ./app
      - name: Build App
        run: pnpm build
        working-directory: ./app

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.config.target }}

      - name: Build Release
        # Buildamo cijeli workspace iz roota
        run: cargo build --workspace --release --target ${{ matrix.config.target }}

      - name: Package Asset
        shell: bash
        run: |
          # Putanja do release binary-a je sada unutar workspace-a
          cd target/${{ matrix.config.target }}/release
          
          # Glavni binary je sada 'valter', a ne 'app'
          cp valter${{ matrix.config.suffix }} ../../../valter-${{ matrix.config.target }}${{ matrix.config.suffix }}
          cd ../../..
      
      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: valter-${{ matrix.config.target }}${{ matrix.config.suffix }}
