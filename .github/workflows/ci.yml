# .github/workflows/ci.yml

name: Valter CI Pipeline (Stable)

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

env:
  CARGO_TERM_COLOR: always
  VALTER_PROVIDER: "gemini"
  VALTER_GEMINI_API_KEY: "ci-dummy-key"
  VALTER_MODEL: "gemini-dummy"
  VALTER_RPM: "30"
  # UPGRADE 1: Konfiguracija za Mold (zadržavamo ovo jer je brzo i sigurno)
  RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
  # UKLONJENO: Sccache (uzrokuje nestabilnost na GitHubu)

jobs:
  # ------------------------------------------------------------------
  # JOB 1: BRZA PROVJERA KVALITETE
  # ------------------------------------------------------------------
  check-quality:
    name: 🔍 Code Quality Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Check Rust Formatting
        run: cargo fmt --all -- --check

  # ------------------------------------------------------------------
  # JOB 2: FRONTEND
  # ------------------------------------------------------------------
  frontend:
    name: 🎨 Frontend Build & Lint
    needs: check-quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'
          cache-dependency-path: 'app/pnpm-lock.yaml'

      - name: Install Dependencies
        working-directory: ./app
        run: pnpm install --frozen-lockfile

      - name: Run Linter
        working-directory: ./app
        run: pnpm lint

      - name: Build Frontend
        working-directory: ./app
        run: pnpm build

  # ------------------------------------------------------------------
  # JOB 3: BACKEND CORE
  # ------------------------------------------------------------------
  backend:
    name: ⚙️ Core Build, Lint & Test
    needs: check-quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # UPGRADE 1: Instaliraj Mold Linker (Brzo linkanje, radi lokalno)
      - name: Set up Mold
        uses: rui314/setup-mold@v1

      # Sistemske ovisnosti (Ubuntu 24.04 fix)
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            pkg-config \
            libssl-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      # Standardni Cargo cache (pouzdan)
      - name: Cache Cargo dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Run Clippy (Strict)
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Run Tests
        run: cargo test --workspace --all-features --no-fail-fast
